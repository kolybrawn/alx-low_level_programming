#include "main.h"
/**
 * main - ests function that prints if integer is positive or negative when given case 0
 * Return: Always 0
 */

int main(void)
{

	int i;

	i = 0;
	positive or negative (i);

	return (0);
}
