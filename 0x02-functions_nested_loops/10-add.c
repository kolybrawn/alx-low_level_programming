#include "main.h"
/**
 * add - function that adds two integers and returns
 * the result
 * @a: first number to add
 * @b: second number to add
 * Return: The result
 */

int add(int a, int b)
{
	return (a + b);
}
